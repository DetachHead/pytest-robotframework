"""
these files match the test names that use them. note that you can't set any breakpoints in any
of these files because these files are not actually executed. instead pytester copies them to a temp
location before they're run
"""
